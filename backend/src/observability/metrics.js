/**
 * DESCONTINUADO - Use prometheus-metrics.js em seu lugar
 * 
 * Este arquivo foi substituído por prometheus-metrics.js que usa
 * a biblioteca prom-client para expor métricas no formato Prometheus.
 * 
 * As funções de registro de métricas foram movidas para prometheus-metrics.js:
 * - recordHttpRequest()
 * - recordDatabaseOperation()
 */

// DEPRECADO - Não use este arquivo
module.exports = {
  // As funções foram movidas para prometheus-metrics.js
};
